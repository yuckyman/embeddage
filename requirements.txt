# embeddage builder dependencies
numpy>=1.24.0
scikit-learn>=1.3.0
wordfreq>=3.0.0

# optional: only needed for preprocessing (generating lemmas.json)
# spaCy is NOT needed for runtime or server deployment
# spacy>=3.7.0

